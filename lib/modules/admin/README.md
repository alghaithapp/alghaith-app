# admin

**Status: migrated** — in-app admin dashboard.

## Layout

```
admin/
├── screens/     # dashboard, tabs, widgets, models
├── services/    # admin_service.dart
└── admin_module.dart
```

Web admin dashboard remains in `src/` (React on Vercel).
